# Database
DATABASE_URL=postgresql://trackrapp:trackrapp@localhost:5432/trackrapp

# SMTP (Mailpit for dev, Resend SMTP for prod)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
SMTP_FROM=noreply@trackrapp.local

# Web Push VAPID keys (generate with: npx web-push generate-vapid-keys)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:hello@trackrapp.local
